import React, {useRef} from 'react';
import styles from './BurgerPresentation.module.css'

const BurgerPresentation = (props: any) => {
    return (
        <div className={styles.card}>
            <h1>Burger Presentation</h1>
        </div>
    );
}

export default BurgerPresentation;
